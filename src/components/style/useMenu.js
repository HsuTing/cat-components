'use strict';

import color, {boxShadow} from 'style/color';

export default {
  menu: {
    padding: '10px',
    color: color.white,
    cursor: 'pointer',
    background: color.blue,
    borderRadius: '50%',
    boxShadow
  }
};
