'use strict';

export const showStyle = {
  maxHeight: '200px',
  overflow: 'scroll'
};

export const hideStyle = {
  maxHeight: '0px',
  overflow: 'hidden'
};

export const root = {
  animation: 'x 0.5s ease-in-out'
};
