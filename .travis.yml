language: node_js
node_js:
  - "node"
before_install:
  - yarn add react react-dom
script:
  - yarn run prod
  - yarn build-color
  - yarn run lint
  - yarn run test
branches:
only:
  - master
yarn: true
